class DeliveriesController < ApplicationController
  # def create
  #   @order = Order.find(params[:order_id])
  #   @delivery = @order.delivery.new(deli_params)
  # end

  # private
  # def deli_params
  #   params.require(:delivery).permit(:price).merge(order_id: params[:order_id])
  # end
end
